name: Generate Playlist

on:
  push:
    paths:
      - 'music/**'
      - '.github/workflows/playlist.yml'
  pull_request:
    paths:
      - 'music/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-playlist:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Debug - List files
      run: |
        echo "=== Archivos MP3 encontrados ==="
        find music -type f -name "*.mp3" | sort
        echo "Total: $(find music -type f -name "*.mp3" | wc -l)"

    - name: Generate playlist.json (CON music/)
      run: |
        echo '{ "tracks": [' > playlist.json
        # RUTAS CON 'music/'
        find music -type f -name "*.mp3" -printf '  "music/%P"\n' | sort | sed '$!s/$/,/' >> playlist.json
        echo '] }' >> playlist.json
        
        echo "=== playlist.json generado ==="
        cat playlist.json

    - name: Commit and push changes
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add playlist.json
        
        if ! git diff --cached --quiet; then
          git commit -m "📻 Auto-update playlist - $(date '+%Y-%m-%d %H:%M')"
          git push
          echo "✅ playlist.json actualizado y subido"
        else
          echo "✅ No hay cambios en playlist.json"
        fi
